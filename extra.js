   //bonus features that were not used but you might find useful

   //attributes for the image instead of the circle, the url, offsets relative to node and the size
   /*node.append("image") 
      .attr("xlink:href", "http://www.clker.com/cliparts/7/c/3/s/q/z/yellow-star-md.png")
      .attr("x", -8)
      .attr("y", -8)
      .attr("width", 16)
      .attr("height", 16);*/




      //NOTE due to multiple instances running at the same time, the links